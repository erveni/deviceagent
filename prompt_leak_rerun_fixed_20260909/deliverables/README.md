# Four corrected Gemini captures — 9 September 2026

All four requested captures are available in results.csv and the four PNG files.
Each shows the complete answer with no prompt text. All four new model outputs
were 4/4 (outside the reported top three). These are new observations, not proof
of the old August positions; original dates and positions are reference columns.

Three captures passed automatically. Scott's answer required a browser-rendered
answer-only screenshot after the paid run: the page could not scroll far enough
to hide its prompt. That recovery reused the same answer, with no new generation,
reload, or proxy session. The original run log remains unchanged at 3 successes.

This repaired pass used 108.288400 MB: 27.072100 MB per delivered capture including
that recovery. The earlier unsuccessful pass used 98.461371 MB, so total spending
across both passes was 206.749771 MB. This is not yet daily-level bandwidth usage.

Only device104 was tested, then restored to production v79. No full ranking queue
restart, fleet deployment, backend upload, or overwrite of historical reports.
The 45 retired Perplexity rows were not run.
